gci -r| sort -descending -property length | select -first 30 fullname, length

gci -r|sort -descending -property length | select -first 30 fullname, @{Name="Gigabytes";Expression={[Math]::round($_.length / 1GB, 2)}}
